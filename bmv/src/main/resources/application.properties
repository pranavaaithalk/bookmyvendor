# --- Server Setup ---
server.port=8081

# --- Spring JPA & Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- Database Connection (MySQL) ---
spring.datasource.url=jdbc:mysql://localhost:3306/bmv?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=pranava
spring.datasource.password=11282004
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

## --- Entity Scanning (optional if packages differ) ---
#spring.jpa.packages-to-scan=com.yourproject.entity

# --- JPA Logging ---
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# --- Timezone Handling ---
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# --- Application Info (optional) ---
spring.application.name=BookMyVendor

# --- Other Common Settings ---
spring.datasource.initialization-mode=always
spring.datasource.continue-on-error=true

## --- (Optional) Flyway or Liquibase settings if using DB migrations ---
## spring.flyway.enabled=true
#
## --- (Optional) Multipart and file upload settings for images ---
#spring.servlet.multipart.max-file-size=10MB
#spring.servlet.multipart.max-request-size=10MB

# --- Twilio SMS ---
#TWILIO_ACCOUNT_SID=${TWILIO_ACCOUNT_SID}
#TWILIO_AUTH_TOKEN=${TWILIO_ACCOUNT_TOKEN}
#TWILIO_PHONE_NUMBER=${TWILIO_ACCOUNT_NUMBER}
DEFAULT_TO_PHONE=+916364248742

# Vendor invite deep link (frontend signup URL base; SMS uses /signup?vendorInvite=<token>)
APP_PUBLIC_BASE_URL=https://bmvindia.online
VENDOR_INVITE_EXPIRY_HOURS=48
# How often to scan for expired pending invites (ms), default 10 minutes
VENDOR_INVITE_EXPIRE_CHECK_MS=600000

# Google Places API Key (set in your environment or here for local dev)
#GOOGLE_PLACES_API_KEY=${GOOGLE_PLACES_API_KEY}


